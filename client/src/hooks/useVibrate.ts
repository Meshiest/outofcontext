// `useHaptics` is the canonical name (returns { vibrate, supported }); `useVibrate` is an alias for
// the same hook.
export { useHaptics, useHaptics as useVibrate } from '@/hooks/useHaptics';
export type { Haptics } from '@/hooks/useHaptics';
