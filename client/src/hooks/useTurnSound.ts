// The implementation lives with the sound data (@/data/sounds); re-exported here so the hooks path
// (@/hooks/useTurnSound) also resolves.
export { useTurnSound, TURN_SOUNDS, soundMap } from '@/data/sounds';
